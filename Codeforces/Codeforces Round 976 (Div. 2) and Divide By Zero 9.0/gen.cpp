#include <bits/stdc++.h>

using namespace std;

#ifdef LOCAL
#include "../debug.h"
#else
#define debug(...) 
#endif

void solve() {
	cout << 100 << endl;
	for (int i = 1; i <= 100; i++)
		cout << i << endl;
}

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	
	freopen("inp.txt", "w", stdout);
	
	int t = 1;
	// cin >> t;
	
	while (t--) {
		solve();
	}
}